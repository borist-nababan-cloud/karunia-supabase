# Local Supabase needs no env vars — `supabase status` prints the local API URL + keys.
#
# These are only needed when pushing the project to Supabase Cloud (production):
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=
# Remote DB URL used by `supabase db push` against a Cloud project:
SUPABASE_DB_URL=
